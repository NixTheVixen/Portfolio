import './HeroBackdrop.css'

// Solid dark-grey backdrop behind the hero (replaces the previous blurred
// Premiere Pro workspace image). Sits behind all content.
export default function HeroBackdrop() {
  return <div className="hero-backdrop" aria-hidden="true" />
}
